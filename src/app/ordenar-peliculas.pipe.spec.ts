import { OrdenarPeliculasPipe } from './ordenar-peliculas.pipe';

describe('OrdenarPeliculasPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarPeliculasPipe();
    expect(pipe).toBeTruthy();
  });
});
